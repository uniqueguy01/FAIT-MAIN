<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="shoe">

	<select id="list" resultType="Shoe">
        SELECT * FROM shoe
    </select>
    
    <insert id="add">
    	INSERT INTO shoe (ceonum, name, price, detail) VALUES (#{ceonum}, #{name}, #{price}, #{detail})
    </insert>
    
    <update id="update">
    	UPDATE shoe
		SET ceonum=#{ceonum}, name=#{name}, price=#{price}, detail=#{detail}
		WHERE id=#{id}
    </update>
    
    <delete id="delete">
    	DELETE FROM shoe
		WHERE id=#{id}
    </delete>

</mapper>