<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="store">

    <select id="list" resultType="Store">
        SELECT * FROM store
    </select>
    
    <insert id="add">
    	INSERT INTO store (ceonum, custid, name) VALUES (#{ceonum}, #{custid}, #{name})
    </insert>
    
    <update id="update">
    	UPDATE store
		SET custid=#{custid}, name=#{name}
		WHERE ceonum=#{ceonum}
    </update>
    
    <delete id="delete">
    	DELETE FROM store
		WHERE ceonum=#{ceonum}
    </delete>
    
</mapper>