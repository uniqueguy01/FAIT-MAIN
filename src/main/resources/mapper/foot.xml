<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="foot">
	
	<select id="list" resultType="Foot">
		SELECT * FROM foot
	</select>
	
	<insert id="add">
		INSERT INTO foot(id, cust_id, foot_size, shape, insole)
		VALUES (FOOT_SEQ.nextval, #{custId}, #{footSize}, #{shape}, #{insole})
	</insert>
	
	<select id="item" resultType="Foot">
		SELECT * FROM foot
		WHERE id=#{id}
	</select>
	
	<update id="update">
		UPDATE foot
		SET cust_id=#{custId}, foot_size=#{footSize}, shape=#{shape}, insole=#{insole}
		WHERE id=#{id}
	</update>
	
	<delete id="delete">
		DELETE FROM foot
		WHERE id=#{id}
	</delete>
	
</mapper>