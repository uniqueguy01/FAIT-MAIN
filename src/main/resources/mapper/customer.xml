<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="customer">

	<select id="list" resultType="Customer">
		SELECT * FROM customer
	</select>
	
	<insert id="add">
		INSERT INTO customer (id, password, name, address, phone, birth)
		VALUES (#{id}, #{password}, #{name}, #{address}, #{phone}, #{birth})
	</insert>
	
	<select id="item" resultType="Customer">
		SELECT * FROM customer
		WHERE id=#{id}
	</select>
	
	<update id="update">
		UPDATE customer
		SET password=#{password}, name=#{name}, address=#{address}, phone=#{phone}, birth=#{birth}
		WHERE id=#{id}
	</update>
	
	<delete id="delete">
		DELETE FROM customer
		WHERE id=#{id}
	</delete>
	
	<select id="login" resultType="Customer">
    	SELECT * FROM customer WHERE id=#{id} AND password=#{password}
	</select>
	
</mapper>